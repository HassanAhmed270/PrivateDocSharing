Progress: initial-skeleton (PrivateAI Agent)

Date: 2026-08-12 (generated by assistant in workspace)

Summary
-------
This file summarizes the one-time initial setup conversation and the current repository state. The goal was to create a minimal, working foundation (backend + frontend scaffolds) on main that four feature branches will branch off from (feat/roles-requests, feat/agent, feat/encryption, feat/frontend).

What I (assistant) did in the workspace
--------------------------------------
- Inspected repository files to confirm the skeleton exists.
- Verified presence of server and client scaffolds (Express ES module backend, Vite+React frontend).
- Did NOT implement any feature work (no auth, users, roles, encryption, agent, etc.).
- Created this progress.md to record the conversation and current state.

Files present (high level)
--------------------------
- server/
  - package.json (type: module)
  - .env.example
  - .env (local placeholder in workspace; do NOT commit this file in real repo)
  - src/
    - app.js
    - server.js
    - config/db.js
    - routes/health.js
    - middleware/error.js
    - (other scaffold folders with .gitkeep)

- client/
  - package.json (Vite + React scaffold)
  - src/
    - main.jsx
    - App.jsx
    - styles.css
  - index.html

- .gitignore (ignores *.env, node_modules, uploads/*, editor files)
- uploads/.gitkeep
- README.md
- progress.md (this file)

Dependencies (declared in package.json files)
--------------------------------------------
- server/package.json dependencies:
  - express
  - mongoose
  - dotenv
  - cors
  - helmet
  - cookie-parser
  - morgan
  - nodemon (devDependency)

- client/package.json dependencies:
  - react, react-dom, vite, @vitejs/plugin-react

Verification steps you (lead) should run locally
------------------------------------------------
1. Backend
   - cd server
   - npm install
   - copy server/.env.example -> .env (set MONGODB_URI if you have MongoDB running)
   - npm run dev
   Expected:
     - On start, the server prints a message about DB connection success or a clear warning if MONGODB_URI is not configured.
     - GET http://localhost:5000/api/health returns:
       { "success": true, "message": "PrivateAI Agent API is running" }

2. Frontend
   - cd client
   - npm install
   - npm run dev
   Expected: Vite dev server starts and shows the scaffolded React page.

Notes about .env
----------------
- server/.env exists in the workspace as a local placeholder (MONGODB_URI set to mongodb://localhost:27017/privateai). In an actual git repository, ensure .gitignore contains "*.env" and do NOT commit real secrets.

What remains to be done by the lead engineer before other developers branch
---------------------------------------------------------------------------
1. Commit the current workspace changes to main with the exact message:
   "chore: initialize backend and frontend project skeleton"
2. Create and push the progress.md change with the commit message:
   "docs: add progress.md"
3. Notify the team that main is ready and they can branch off:
   - feat/roles-requests
   - feat/agent
   - feat/encryption
   - feat/frontend

Suggested commit commands (run locally)
---------------------------------------
# From repository root
git add .
git commit -m "chore: initialize backend and frontend project skeleton"
git push origin main

# Add progress.md (if not already included above)
git add progress.md
git commit -m "docs: add progress.md"
git push origin main

Limitations / Assumptions
-------------------------
- This assistant could not run git commands or push to GitHub from this environment. The repository changes exist in the workspace and must be committed and pushed by the lead engineer in their environment.
- The included server/.env in the workspace is a placeholder. Ensure real .env files with secrets are never pushed.
- No feature work was implemented. Feature branches must implement their own logic.

Next recommended actions for the lead engineer
---------------------------------------------
1. Review files in workspace and run the verification steps above locally.
2. Commit and push to main using the exact messages requested by the team process.
3. Create and protect branch naming policy if desired.
4. Notify the team they may branch off main and begin feature work.

End of progress.md


STAGE 1 — Central API Service (prepared locally, ready to push)
---------------------------------------------------------------
Date: 2026-08-12

Summary of changes prepared for Stage 1
- Added a central axios instance at client/src/services/api.js (configured to use import.meta.env.VITE_API_BASE_URL).
- The instance includes a request interceptor that attaches Authorization: Bearer <token> from localStorage when present.
- The instance includes a response interceptor that handles 401/403 centrally (redirects to /login on 401 and 403) and removes stale token on 401.
- Confirmed axios is present in client/package.json dependencies ("axios": "^1.4.0").
- Updated this progress.md with a Stage 1 entry. A placeholder commit hash is included below; replace it after you push.

Files to commit
- client/src/services/api.js
- client/package.json (axios dependency already present; include in commit to be explicit)
- progress.md (this file, with the Stage 1 entry and placeholder)

Placeholder commit hash
- <COMMIT_HASH_HERE>

Suggested git commands to run locally (from repo root)
1) Stage the prepared files:
   git add client/src/services/api.js client/package.json progress.md
2) Commit with the required message:
   git commit -m "feat: add central api service instance"
3) Push to the current branch (replace origin and branch if needed):
   git push origin HEAD

After pushing
- Replace <COMMIT_HASH_HERE> above with the real commit hash (e.g. run `git rev-parse HEAD` locally and edit progress.md), then commit and push that change or include it in the same commit before pushing.

Verification (quick checks)
- In client, run:
    cd client
    npm install
    npm run dev
  Then in code, importing the service should not throw (example):
    import api from './services/api'
    // api is an axios instance and can be used by components/pages

Notes / Assumptions
- The workspace already contained client/src/services/api.js and axios in client/package.json; no code changes to api.js were necessary beyond verification.
- I cannot run git commands or push from this environment. You must run the git commands and push from your machine. After you push, update progress.md to include the real commit hash.
- Do NOT implement auth pages or auth context in this stage per your instructions.

End of Stage 1 entry
